{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "version": 2,
  "buildCommand": "cd app && npm install && NODE_OPTIONS='--max_old_space_size=4096' npm run build",
  "outputDirectory": "app/.next",
  "installCommand": "cd app && npm install",
  "env": {
    "NODE_ENV": "production"
  }
}
